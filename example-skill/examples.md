# Example Skill — Examples

## Example 1: Greeting

**User:** Show me how this example skill works.

**Agent:**

1. Summarize: "I would acknowledge the request and explain this is a demo skill."
2. Do not modify production systems or external tools.

## Example 2: Wrong skill name

If a folder is named `my_skill` but frontmatter says `name: my-skill`, fix the folder or the `name` field so they match [Agent Skills spec](https://agentskills.io/specification).
